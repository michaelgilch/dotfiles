# i3 config file 
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# -----------------------------
# Look and Feel
# -----------------------------

default_border          pixel 3
default_floating_border pixel 3
gaps inner 12
smart_gaps on

font pango:DejaVu Sans Mono 10

focus_follows_mouse no

# -----------------------------
# Autostart
# -----------------------------

exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id ~/bin/polybar-launch.sh
exec --no-startup-id dunst
exec --no-startup-id udiskie
exec --no-startup-id solaar -w hide

# Compositor
exec --no-startup-id exec picom -b
# Background
exec --no-startup-id exec ~/bin/set-wallpaper
# Lock on suspend
exec --no-startup-id xss-lock --transfer-sleep-lock -- ~/bin/i3-lock 
# Network Manager Applet - TODO do I need this?
exec --no-startup-id nm-applet
# Screenshots
exec --no-startup-id flameshot

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Force workspace creation with proper names at startup
exec --no-startup-id "i3-msg 'workspace \"1: Home\"; workspace \"2: Util\"; workspace \"1: Home\"'"

# =============================
# Keyboard Shortcuts
# =============================

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m '' -B 'Exit i3' 'i3-msg exit'"

# kill focused window
bindsym $mod+Shift+q kill

# Lock Screen
bindsym $mod+Shift+x exec --no-startup-id ~/bin/i3-lock 

# jgmenu
bindsym $mod+Ctrl+Left exec --no-startup-id ~/bin/jg-sidepanel left
bindsym $mod+Ctrl+Right exec --no-startup-id ~/bin/jg-sidepanel right

# ---------------------
# Applications
# ---------------------
bindsym $mod+Return exec i3-sensible-terminal

bindsym $mod+d exec "rofi -modi drun,run,window -show drun"

bindsym Print exec "flameshot gui"
bindsym $mod+Shift+w exec "google-chrome-stable"
bindsym $mod+Shift+t exec "thunar"

bindsym $mod+x exec jgmenu_run

# ---------------------
# Navigation
# ---------------------

# Change window focus via vim keys
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# Change window focus via arrow keys
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move focused window via vim keys
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# Move focused window via arrow keys
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Split Horizontal
bindsym $mod+b split h
# Split Vertical 
bindsym $mod+v split v

bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

set $ws1 "1: Home"
set $ws2 "2: Util"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# Switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# Move focused container
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# Change workspace names
bindsym $mod+n exec ~/bin/i3-rename-workspace

for_window [class="TeamViewer" title="^TeamViewer$"] floating enable
for_window [class="Qalculate-gtk"] floating enable
for_window [title="System Updates"] floating enable, resize set 800 600, move position center, move to output DP-1

# Resize Window
mode "resize" {
        bindsym h resize shrink width  5 px or 5 ppt
        bindsym j resize grow   height 5 px or 5 ppt
        bindsym k resize shrink height 5 px or 5 ppt
        bindsym l resize grow   width  5 px or 5 ppt

        bindsym Left  resize shrink width  5 px or 5 ppt
        bindsym Down  resize grow   height 5 px or 5 ppt
        bindsym Up    resize shrink height 5 px or 5 ppt
        bindsym Right resize grow   width  5 px or 5 ppt

        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Load host-specific configurations
include ~/.config/i3/config.d/$(hostnamectl hostname)
